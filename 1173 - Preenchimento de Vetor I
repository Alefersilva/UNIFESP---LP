#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, a, N[10];
    scanf("%d", &a);
    N[0]=a;
        for(i=1;i<10;i++)
        {
            N[i]=N[i-1]*2;
        }
        for(i=0;i<10;i++)
        {
            printf("N[%i] = %i\n",i,N[i]);
        }
    return 0;
}
